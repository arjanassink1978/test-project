# Reuse containers: disabled by default (clean database per test run)
# Only enable for PIT mutation testing (transactions guarantee isolation):
#   TESTCONTAINERS_REUSE_ENABLE=true mvn pitest:mutationCoverage -pl restassured-tests
#
# Normal integration tests MUST have reuse disabled to prevent data pollution.
testcontainers.reuse.enable=false
