# Reuse containers across test runs (faster for PIT mutation testing)
# WARNING: Only safe for PIT (uses transactions + rollback per mutation).
# For normal integration tests with data changes, set to false:
#   TESTCONTAINERS_REUSE_ENABLE=false mvn test -pl restassured-tests
testcontainers.reuse.enable=true
